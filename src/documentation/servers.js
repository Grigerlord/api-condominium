// import dotenv from 'dotenv'

// dotenv.config()
module.exports = {
  servers: [
    {
      // url: `${process.env.API_HOST}:${process.env.API_PORT}`,
      url: `http://localhost:9000`,
      description: "Condostart API",
    },
  ],
};